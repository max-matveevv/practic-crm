# 🚀 Файлы деплоя PracticCRM

Эта папка содержит все необходимые файлы для деплоя проекта на CloudPanel.

## 📁 Содержимое папки:

- **`DEPLOYMENT.md`** - подробная инструкция по деплою на CloudPanel
- **`deploy.sh`** - скрипт автоматического деплоя
- **`nginx.conf`** - конфигурация Nginx для CloudPanel
- **`ecosystem.config.js`** - конфигурация PM2 для управления процессами
- **`env.production.example`** - пример переменных окружения для backend
- **`frontend.env.production.example`** - пример переменных окружения для frontend

## 🚀 Быстрый старт:

1. Прочитайте **`DEPLOYMENT.md`** для полной инструкции
2. Настройте сайт в CloudPanel
3. Скопируйте файлы на сервер
4. Запустите `deploy.sh`

## 📝 Важно:

- Замените `crm.practic.studio` на ваш реальный поддомен во всех файлах
- Настройте GitHub Secrets для автоматического деплоя
- Убедитесь, что PM2 установлен на сервере
