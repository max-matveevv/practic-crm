# CRM System

Система управления проектами и задачами с авторизацией пользователей, поддержкой изображений и лайтбоксом.

## 🏗️ Структура проекта

```
practic-crm/
├── backend/                    # Laravel API (PHP)
│   ├── app/
│   │   ├── Http/Controllers/   # Контроллеры API
│   │   │   ├── AuthController.php      # Авторизация, профиль, сброс пароля
│   │   │   ├── ProjectController.php   # CRUD проектов
│   │   │   ├── TaskController.php      # CRUD задач
│   │   │   └── FileUploadController.php # Загрузка изображений
│   │   ├── Models/             # Eloquent модели
│   │   │   ├── User.php                # Модель пользователя
│   │   │   ├── Project.php             # Модель проекта
│   │   │   └── Task.php                # Модель задачи (с поддержкой изображений)
│   │   └── Providers/          # Провайдеры сервисов
│   ├── config/                 # Конфигурация
│   │   ├── sanctum.php         # Настройки Laravel Sanctum
│   │   ├── cors.php            # CORS настройки
│   │   └── auth.php            # Настройки авторизации
│   ├── .env                    # Переменные окружения (включая путь к БД)
│   ├── database/
│   │   └── migrations/         # Миграции БД
│   ├── routes/
│   │   ├── api.php             # API маршруты
│   │   └── web.php             # Web маршруты (CSRF, Telescope)
│   ├── storage/app/public/task-images/ # Загруженные изображения
│   └── ...
├── frontend/                   # Next.js приложение (TypeScript)
│   ├── src/
│   │   ├── app/                # App Router (Next.js 14)
│   │   │   ├── (protected)/    # Защищенные страницы
│   │   │   │   ├── layout.tsx          # Лейаут с авторизацией
│   │   │   │   ├── dashboard/          # Дашборд
│   │   │   │   │   └── page.tsx        # Главная страница
│   │   │   │   ├── projects/           # Страницы проектов
│   │   │   │   │   ├── page.tsx        # Список проектов
│   │   │   │   │   └── [id]/           # Детали проекта
│   │   │   │   │       └── page.tsx    # Страница проекта
│   │   │   │   └── tasks/              # Страницы задач
│   │   │   │       ├── page.tsx        # Server component
│   │   │   │       └── TasksPageClient.tsx # Client component
│   │   │   ├── (public)/       # Публичные страницы
│   │   │   │   ├── layout.tsx          # Лейаут без авторизации
│   │   │   │   ├── page.tsx            # Главная страница
│   │   │   │   ├── auth/               # Страница авторизации
│   │   │   │   │   ├── layout.tsx      # Layout для авторизации
│   │   │   │   │   └── page.tsx        # Формы входа/регистрации
│   │   │   │   └── reset-password/     # Страница сброса пароля
│   │   │   │       ├── layout.tsx      # Layout для сброса пароля
│   │   │   │       └── page.tsx        # Формы сброса пароля
│   │   │   └── layout.tsx      # Корневой лейаут
│   │   ├── components/         # React компоненты
│   │   │   ├── TaskCard/       # Компоненты для работы с задачами
│   │   │   │   ├── TaskCard.tsx        # Основной компонент карточки
│   │   │   │   ├── TaskCardView.tsx    # Режим просмотра с лайтбоксом
│   │   │   │   ├── TaskCardEdit.tsx    # Режим редактирования
│   │   │   │   └── AddTaskForm.tsx     # Форма добавления задачи
│   │   │   ├── ProjectCard/    # Компоненты для работы с проектами
│   │   │   │   ├── ProjectCard.tsx     # Карточка проекта
│   │   │   │   └── AddProjectForm.tsx  # Форма добавления проекта
│   │   │   ├── auth/           # Компоненты авторизации
│   │   │   │   ├── LoginForm.tsx       # Форма входа
│   │   │   │   └── RegisterForm.tsx    # Форма регистрации
│   │   │   ├── elements/       # UI компоненты
│   │   │   │   ├── Header.tsx          # Шапка сайта
│   │   │   │   ├── Sidebar.tsx         # Боковая панель
│   │   │   │   └── Footer.tsx          # Подвал
│   │   │   └── ui/             # Универсальные UI компоненты
│   │   │       ├── Button.tsx          # Универсальная кнопка
│   │   │       ├── Input.tsx           # Универсальный input
│   │   │       ├── Textarea.tsx        # Универсальный textarea
│   │   │       ├── Select.tsx          # Кастомный select
│   │   │       ├── ImageUpload.tsx     # Компонент загрузки изображений
│   │   │       └── Lightbox.tsx        # Лайтбокс для просмотра изображений
│   │   ├── api/                # API клиенты
│   │   │   ├── common.ts               # Общие функции API
│   │   │   ├── auth.ts                 # API авторизации
│   │   │   ├── projects.ts             # API проектов
│   │   │   └── tasks.ts                # API задач
│   │   ├── contexts/           # React Context
│   │   │   └── AuthContext.tsx         # Контекст авторизации
│   │   ├── lib/                # Утилиты и типы
│   │   │   ├── types.ts                # TypeScript типы (включая TaskImage)
│   │   │   └── metadata.ts             # Утилита для метаданных
│   │   └── middleware.ts       # Next.js middleware
│   ├── public/                 # Статические файлы
│   │   ├── favicon.ico         # Фавикон
│   │   ├── favicon-16x16.png   # Фавикон 16x16
│   │   ├── favicon-32x32.png   # Фавикон 32x32
│   │   ├── apple-touch-icon.png # Иконка для iOS
│   │   ├── android-chrome-192x192.png # Иконка для Android
│   │   ├── android-chrome-512x512.png # Иконка для Android
│   │   └── site.webmanifest    # Web App Manifest
│   └── ...
├── data/                       # Данные приложения
│   └── database.sqlite         # База данных SQLite
├── deploy/                     # Файлы для деплоя
│   ├── production.env.example  # Пример .env для продакшена
│   ├── frontend.env.production.example # Пример .env для фронтенда
│   ├── deploy.sh               # Скрипт деплоя
│   ├── nginx.conf              # Конфигурация Nginx
│   └── ecosystem.config.js     # Конфигурация PM2
└── README.md
```

## 🚀 Запуск проекта

### Требования
- PHP 8.1+
- Composer
- Node.js 18+
- SQLite

### Backend (Laravel)

```bash
cd backend

# Установка зависимостей
composer install

# Настройка окружения
cp .env.example .env
php artisan key:generate

# Настройка базы данных
# База данных будет создана автоматически в папке data/
php artisan migrate

# Настройка символической ссылки для storage
php artisan storage:link

# Запуск сервера
php artisan serve
```

Backend будет доступен на `http://localhost:8000`

### Frontend (Next.js)

```bash
cd frontend

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 🔧 Настройка для продакшена

### Backend (.env)
```env
APP_URL=https://crm.practic.studio
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax
SESSION_PARTITIONED_COOKIE=true
SESSION_DOMAIN=.practic.studio
SANCTUM_STATEFUL_DOMAINS=crm.practic.studio
```

### Frontend (.env.local)
```env
NEXT_PUBLIC_API_URL=https://crm.practic.studio/api
```

### Команды после деплоя
```bash
php artisan config:clear
php artisan config:cache
php artisan route:cache
php artisan storage:link
```

## 📋 Функциональность

- **Авторизация**: регистрация, вход, выход, сброс пароля
- **Проекты**: создание, редактирование, удаление
- **Задачи**: создание, изменение статуса, привязка к проектам, сортировка
- **Изображения**: загрузка изображений к задачам с лайтбоксом для просмотра
- **Фильтрация**: по проектам и статусам с кастомными select'ами
- **Пользователи**: каждый видит только свои данные
- **UI компоненты**: универсальные Button, Input, Textarea, Select, ImageUpload, Lightbox
- **Темная тема**: поддержка dark mode во всех компонентах
- **Метаданные**: SEO оптимизация с фавиконами и манифестом
- **Лайтбокс**: полноэкранный просмотр изображений с навигацией

## 🛠️ Технологии

### Backend
- Laravel 11
- Laravel Sanctum (авторизация)
- SQLite (база данных)
- Laravel Telescope (отладка)
- File Storage (загрузка изображений)

### Frontend
- Next.js 14
- TypeScript
- Tailwind CSS
- React Context (состояние)

## 🏗️ Компоненты системы

### 🔧 Backend (Laravel)

#### **Контроллеры**
- **`AuthController.php`** - Обработка авторизации, профиля, сброса пароля
  - `register()` - Регистрация нового пользователя
  - `login()` - Вход в систему с созданием токена
  - `logout()` - Выход и удаление токена
  - `me()` - Получение данных текущего пользователя
  - `updateProfile()` - Обновление профиля
  - `changePassword()` - Смена пароля
  - `sendPasswordResetLink()` - Отправка ссылки сброса пароля
  - `resetPassword()` - Сброс пароля по токену

- **`ProjectController.php`** - CRUD операции с проектами
  - `index()` - Список проектов пользователя
  - `store()` - Создание нового проекта
  - `show()` - Получение проекта по ID
  - `destroy()` - Удаление проекта

- **`TaskController.php`** - CRUD операции с задачами
  - `index()` - Список задач с фильтрацией
  - `store()` - Создание новой задачи
  - `show()` - Получение задачи по ID
  - `update()` - Обновление задачи
  - `destroy()` - Удаление задачи
  - `projectTasks()` - Задачи конкретного проекта

- **`FileUploadController.php`** - Загрузка изображений
  - `uploadImages()` - Загрузка изображений для задач
  - Валидация типов файлов (jpeg, png, gif, webp)
  - Ограничение размера файлов (5MB)
  - Возврат полных URL изображений

#### **Модели (Eloquent)**
- **`User.php`** - Модель пользователя
  - Связи: `hasMany(Project)`, `hasMany(Task)`
  - Трейты: `HasApiTokens`, `Notifiable`

- **`Project.php`** - Модель проекта
  - Связи: `belongsTo(User)`, `hasMany(Task)`
  - Поля: title, description, login, password, url, etc.

- **`Task.php`** - Модель задачи
  - Связи: `belongsTo(User)`, `belongsTo(Project)`
  - Поля: title, description, status, priority, images (JSON)
  - Casts: `'images' => 'array'`

#### **Маршруты**
- **`api.php`** - API маршруты с middleware `auth:sanctum`
  - `/api/upload/images` - Загрузка изображений
- **`web.php`** - Web маршруты (CSRF, Telescope)

#### **База данных**
- **`data/database.sqlite`** - База данных SQLite (вне папки backend)
- **`.env`** - Конфигурация пути к БД: `DB_DATABASE=../../data/database.sqlite`
- **Преимущества**: 
  - Отделение данных от кода
  - Упрощение бэкапов
  - Лучшая организация проекта

### 🎨 Frontend (Next.js)

#### **Страницы (App Router)**
- **`(protected)/`** - Защищенные страницы
  - `layout.tsx` - Лейаут с проверкой авторизации
  - `dashboard/page.tsx` - Главная страница с дашбордом
  - `projects/page.tsx` - Список проектов
  - `projects/[id]/page.tsx` - Детали проекта
  - `tasks/page.tsx` - Server component для метаданных
  - `tasks/TasksPageClient.tsx` - Client component с логикой

- **`(public)/`** - Публичные страницы
  - `layout.tsx` - Лейаут без авторизации
  - `page.tsx` - Главная страница для неавторизованных
  - `auth/layout.tsx` - Layout для авторизации
  - `auth/page.tsx` - Страница авторизации
  - `reset-password/layout.tsx` - Layout для сброса пароля
  - `reset-password/page.tsx` - Сброс пароля

#### **Компоненты**

##### **TaskCard/** - Компоненты для работы с задачами
- **`TaskCard.tsx`** - Основной компонент, управляющий состоянием
  - Переключение между режимами просмотра и редактирования
  - Управление состоянием лайтбокса
  - Обработка CRUD операций

- **`TaskCardView.tsx`** - Режим просмотра задачи
  - Отображение информации о задаче
  - Кнопки редактирования и удаления
  - Лайтбокс для просмотра изображений
  - Быстрая смена статуса через Select

- **`TaskCardEdit.tsx`** - Режим редактирования задачи
  - Форма редактирования всех полей задачи
  - Компонент ImageUpload для управления изображениями
  - Валидация и сохранение изменений

- **`AddTaskForm.tsx`** - Форма добавления новой задачи
  - Все поля задачи включая изображения
  - Привязка к проектам
  - Валидация перед отправкой

##### **ProjectCard/** - Компоненты для работы с проектами
- **`ProjectCard.tsx`** - Карточка проекта с данными доступа
- **`AddProjectForm.tsx`** - Форма добавления проекта

##### **auth/** - Компоненты авторизации
- **`LoginForm.tsx`** - Форма входа с валидацией
- **`RegisterForm.tsx`** - Форма регистрации

##### **elements/** - UI компоненты
- **`Header.tsx`** - Шапка с навигацией и профилем
- **`Sidebar.tsx`** - Боковая панель с меню
- **`Footer.tsx`** - Подвал сайта

##### **ui/** - Универсальные UI компоненты
- **`Button.tsx`** - Универсальная кнопка (primary, secondary, danger, ghost)
- **`Input.tsx`** - Универсальный input с валидацией и темной темой
- **`Textarea.tsx`** - Универсальный textarea
- **`Select.tsx`** - Кастомный select с поиском и темной темой
- **`ImageUpload.tsx`** - Компонент загрузки изображений
  - Drag & drop интерфейс
  - Предварительный просмотр
  - Валидация типов и размеров файлов
  - Удаление изображений
  - Загрузка на сервер с прогрессом
- **`Lightbox.tsx`** - Лайтбокс для просмотра изображений
  - Полноэкранный режим
  - Навигация между изображениями
  - Клавиатурное управление (Escape, стрелки)
  - Отображение информации о файле

#### **API клиенты**
- **`common.ts`** - Общие функции API
  - `getToken()` - Получение токена из localStorage
  - `getCsrfToken()` - Получение CSRF токена из cookies
  - `getAuthHeaders()` - Формирование заголовков авторизации
  - `apiRequest()` - Базовый запрос к API

- **`auth.ts`** - API авторизации
  - `login()` - Вход в систему
  - `register()` - Регистрация
  - `logout()` - Выход
  - `getCurrentUser()` - Получение текущего пользователя
  - `sendPasswordResetLink()` - Запрос сброса пароля
  - `resetPassword()` - Сброс пароля

- **`projects.ts`** - API проектов
  - `fetchProjects()` - Получение списка проектов
  - `createProject()` - Создание проекта
  - `fetchProject()` - Получение проекта
  - `deleteProject()` - Удаление проекта

- **`tasks.ts`** - API задач
  - `fetchTasks()` - Получение списка задач
  - `createTask()` - Создание задачи
  - `updateTask()` - Обновление задачи
  - `deleteTask()` - Удаление задачи

#### **Контекст и состояние**
- **`AuthContext.tsx`** - React Context для авторизации
  - Состояние: `isAuthenticated`, `user`, `loading`
  - Методы: `login()`, `logout()`, `register()`

#### **Утилиты**
- **`types.ts`** - TypeScript типы
  - `TaskImage` - Тип для изображений задач
  - `Task` - Расширенный тип задачи с массивом изображений
  - `Project`, `User` - Основные типы

- **`metadata.ts`** - Утилита для метаданных
  - `createMetadata()` - Функция создания метаданных
  - `metadataConfigs` - Предустановленные конфигурации для разных страниц
  - Поддержка SEO, фавиконов, манифеста

#### **Middleware**
- **`middleware.ts`** - Next.js middleware
  - Проверка токена в cookies
  - Перенаправление на `/auth` если не авторизован
  - Защита маршрутов `(protected)/*`
  - Автоматический редирект на `/tasks` после авторизации

## 🖼️ Работа с изображениями

### 📤 Загрузка изображений
1. **Выбор файлов**: Drag & drop или кнопка выбора
2. **Валидация**: Проверка типа (jpeg, png, gif, webp) и размера (до 5MB)
3. **Предварительный просмотр**: Отображение миниатюр
4. **Загрузка на сервер**: POST запрос с FormData
5. **Сохранение**: Ссылки на изображения сохраняются в задаче

### 🖼️ Лайтбокс для просмотра
1. **Открытие**: Клик по изображению в карточке задачи
2. **Навигация**: Стрелки влево/вправо или клавиши ←/→
3. **Закрытие**: Escape, клик по фону или кнопка закрытия
4. **Информация**: Отображение названия файла и счетчика

### 🔧 Технические детали
- **Backend**: Laravel Storage с символической ссылкой
- **Frontend**: FormData для загрузки, полные URL для отображения
- **Безопасность**: CSRF токены, валидация файлов
- **Производительность**: Ленивая загрузка изображений

## 🎯 API Endpoints

### 🔐 Авторизация (Public)
| Метод | Endpoint | Описание | Заголовки |
|-------|----------|----------|-----------|
| `POST` | `/api/auth/register` | Регистрация нового пользователя | `X-XSRF-TOKEN`, `X-Requested-With` |
| `POST` | `/api/auth/login` | Вход в систему | `X-XSRF-TOKEN`, `X-Requested-With` |
| `POST` | `/api/auth/forgot-password` | Запрос ссылки сброса пароля | `X-XSRF-TOKEN`, `X-Requested-With` |
| `POST` | `/api/auth/reset-password` | Сброс пароля по токену | `X-XSRF-TOKEN`, `X-Requested-With` |

### 🔒 Авторизация (Protected)
| Метод | Endpoint | Описание | Заголовки |
|-------|----------|----------|-----------|
| `POST` | `/api/auth/logout` | Выход из системы | `Authorization: Bearer`, `X-XSRF-TOKEN` |
| `GET` | `/api/auth/me` | Получение данных текущего пользователя | `Authorization: Bearer`, `X-XSRF-TOKEN` |
| `PUT` | `/api/auth/profile` | Обновление профиля | `Authorization: Bearer`, `X-XSRF-TOKEN` |
| `POST` | `/api/auth/change-password` | Смена пароля | `Authorization: Bearer`, `X-XSRF-TOKEN` |

### 📁 Проекты (Protected)
| Метод | Endpoint | Описание | Параметры |
|-------|----------|----------|-----------|
| `GET` | `/api/projects` | Список проектов пользователя | - |
| `POST` | `/api/projects` | Создание нового проекта | `title`, `description`, `login`, `password`, `url`, etc. |
| `GET` | `/api/projects/{id}` | Получение проекта по ID | - |
| `DELETE` | `/api/projects/{id}` | Удаление проекта | - |

### ✅ Задачи (Protected)
| Метод | Endpoint | Описание | Параметры |
|-------|----------|----------|-----------|
| `GET` | `/api/tasks` | Список задач пользователя | `?project_id`, `?status` |
| `POST` | `/api/tasks` | Создание новой задачи | `title`, `description`, `status`, `project_id`, `priority`, `images` |
| `PUT` | `/api/tasks/{id}` | Обновление задачи | `title`, `description`, `status`, `project_id`, `priority`, `images` |
| `DELETE` | `/api/tasks/{id}` | Удаление задачи | - |
| `GET` | `/api/tasks/projects/{project}` | Задачи конкретного проекта | - |

### 🖼️ Файлы (Protected)
| Метод | Endpoint | Описание | Параметры |
|-------|----------|----------|-----------|
| `POST` | `/api/upload/images` | Загрузка изображений для задач | `images[]` (FormData) |

### 🌐 Web Routes
| Метод | Endpoint | Описание | Назначение |
|-------|----------|----------|------------|
| `GET` | `/sanctum/csrf-cookie` | Получение CSRF cookie | Установка XSRF-TOKEN |
| `GET` | `/telescope` | Laravel Telescope (prod) | Отладка и мониторинг |

## 🗄️ Структура базы данных

### Таблица `users`
```sql
- id (primary key)
- name (string)
- email (string, unique)
- email_verified_at (timestamp, nullable)
- password (hashed string)
- remember_token (string, nullable)
- created_at (timestamp)
- updated_at (timestamp)
```

### Таблица `projects`
```sql
- id (primary key)
- title (string)
- description (text, nullable)
- login (string, nullable)
- password (string, nullable)
- url (string, nullable)
- accesses (text, nullable)
- admin_url (string, nullable)
- admin_login (string, nullable)
- admin_password (string, nullable)
- ssh_host (string, nullable)
- ssh_user (string, nullable)
- ssh_password (string, nullable)
- ssh_port (integer, nullable)
- build_commands (text, nullable)
- notes (text, nullable)
- user_id (foreign key → users.id)
- created_at (timestamp)
- updated_at (timestamp)
```

### Таблица `tasks`
```sql
- id (primary key)
- title (string)
- description (text, nullable)
- status (enum: pending, in_progress, completed)
- priority (integer: 1-3)
- project_id (foreign key → projects.id, nullable)
- user_id (foreign key → users.id)
- images (text, nullable) -- JSON массив изображений
- created_at (timestamp)
- updated_at (timestamp)
```

### Таблица `password_reset_tokens`
```sql
- email (primary key)
- token (hashed string)
- created_at (timestamp)
```

## 🔧 Конфигурационные файлы

### Backend
- **`.env`** - Основные настройки приложения (включая путь к БД)
- **`config/sanctum.php`** - Настройки Laravel Sanctum
- **`config/cors.php`** - CORS конфигурация
- **`config/auth.php`** - Настройки авторизации
- **`data/database.sqlite`** - База данных SQLite (отдельная папка)

### Frontend
- **`.env.local`** - Локальные переменные окружения
- **`.env.production`** - Продакшен переменные
- **`next.config.ts`** - Конфигурация Next.js
- **`middleware.ts`** - Middleware для авторизации

### Deploy
- **`deploy.sh`** - Скрипт автоматического деплоя
- **`nginx.conf`** - Конфигурация веб-сервера
- **`ecosystem.config.js`** - Конфигурация PM2

## 🔧 Troubleshooting

### ❌ Частые проблемы

#### **CSRF token mismatch**
```
Ошибка: 419 CSRF token mismatch
```
**Решение:**
1. Проверить настройки в `config/sanctum.php`
2. Убедиться что домен добавлен в `stateful`
3. Проверить CORS настройки в `config/cors.php`
4. Убедиться что `supports_credentials: true`

#### **401 Unauthorized**
```
Ошибка: 401 Unauthorized
```
**Решение:**
1. Проверить что токен сохранен в localStorage
2. Убедиться что заголовок `Authorization: Bearer {token}` отправляется
3. Проверить что токен не истек
4. Переавторизоваться

#### **405 Method Not Allowed**
```
Ошибка: 405 Method Not Allowed
```
**Решение:**
1. Проверить что API URL настроен правильно
2. Убедиться что маршрут существует в `routes/api.php`
3. Проверить HTTP метод в запросе

#### **Database locked**
```
Ошибка: SQLSTATE[HY000]: General error: 5 database is locked
```
**Решение:**
1. Остановить все процессы Laravel
2. Перезапустить сервер
3. Проверить права доступа к файлу БД в папке `data/`

#### **База данных не найдена**
```
Ошибка: SQLSTATE[HY000]: General error: 1 no such table
```
**Решение:**
1. Проверить что файл `data/database.sqlite` существует
2. Убедиться что путь в `.env` правильный: `DB_DATABASE=../../data/database.sqlite`
3. Выполнить `php artisan migrate` для создания таблиц

#### **Изображения не загружаются**
```
Ошибка: 404 Not Found для изображений
```
**Решение:**
1. Выполнить `php artisan storage:link`
2. Проверить что папка `storage/app/public` существует
3. Проверить права доступа к папке storage
4. Убедиться что APP_URL настроен правильно

### 🛠️ Отладка

#### **Laravel Telescope**
```bash
# В продакшене
php artisan telescope:user telescope@crm.practic.studio telescope123
# Доступ: https://crm.practic.studio/telescope
```

#### **Логи Laravel**
```bash
# Просмотр логов
tail -f storage/logs/laravel.log
```

#### **Проверка токенов**
```bash
# Laravel Tinker
php artisan tinker
>>> $user = User::find(1)
>>> $user->tokens
```

#### **Проверка миграций**
```bash
# Статус миграций
php artisan migrate:status

# Запуск миграций
php artisan migrate

# Проверка структуры таблиц
php artisan tinker
>>> DB::select('PRAGMA table_info(tasks)')
```

### 📋 Чек-лист деплоя

#### **Backend**
- [ ] `.env` настроен для продакшена
- [ ] `APP_URL` указан правильный домен
- [ ] `SESSION_SECURE_COOKIE=true`
- [ ] `SESSION_SAME_SITE=lax`
- [ ] `SANCTUM_STATEFUL_DOMAINS` содержит домен
- [ ] `DB_DATABASE` указывает на правильный путь к БД
- [ ] База данных создана и мигрирована в папке `data/`
- [ ] Права доступа к папкам настроены (включая `data/`)
- [ ] PM2/Nginx настроены
- [ ] `php artisan storage:link` выполнен

#### **Frontend**
- [ ] `.env.production` настроен
- [ ] `NEXT_PUBLIC_API_URL` указан правильный API
- [ ] Сборка прошла без ошибок
- [ ] PM2 настроен для frontend
- [ ] Nginx проксирует на frontend

### 🔄 Команды для деплоя

```bash
# Backend
cd backend
composer install --optimize-autoloader --no-dev
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan migrate --force
php artisan storage:link

# Frontend
cd frontend
npm ci --production
npm run build
```
